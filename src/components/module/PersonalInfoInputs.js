import { personalInfoSchema } from "@/lib/constants";
import Calendar from "@/public/icons/Calendar";
import { useUpdateData } from "../core/services/mutation";
import { useForm } from "react-hook-form";

function PersonalInfoInputs({ setShowPersonalInfo }) {
  // const { mutate, isPending } = useUpdateData();
  // const { shaba_code, accountIdentifier, debitCard_code } = data?.payment;
 
  return (
    <div>
     
    </div>
  );
}

export default PersonalInfoInputs;
